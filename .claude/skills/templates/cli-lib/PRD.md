# PRD: {프로젝트명}

> **이 문서가 답하는 질문**: 이 도구/라이브러리는 *어떤 개발자에게 어떤 표면(CLI/Lib)*으로 *어떤 호환 정책*을 약속하는가? 공개 API 시그니처는 [API_REFERENCE.md](./API_REFERENCE.md), 시스템 구조는 [ARCHITECTURE.md](./ARCHITECTURE.md).

## 목표
{이 도구/라이브러리가 해결하려는 문제를 한 줄로}

## 사용자
- 1차 사용자: {다른 개발자 — 어떤 직군 / 어떤 환경에서}
- 사용 시나리오: {예: 빌드 파이프라인 / 로컬 디버깅 / 다른 코드에서 import}

## 공개 인터페이스
- 형태: {CLI 서브커맨드 / 라이브러리 함수 / 둘 다}
- 핵심 진입점:
  1. {예: `tool init <path>` — 초기화}
  2. {예: `tool run --config X` — 실행}
  3. {예: `from tool import process` — 라이브러리 호출}

## 범위 외 (Out of scope)
공개 API 표면에 들어가지 않을 기능. 1개 이상 — 안 정하면 호환성 약속이 흐려진다.
- {예: GUI 모드 / 외부 네트워크 호출 / 플러그인 시스템}
- {}

## 비기능 요건
- 호환 대상: {예: Python 3.10+ / Node 20+ / cargo MSRV 1.78}
- 오프라인 동작: {예: 외부 네트워크 없이 동작 / 일부 명령은 네트워크 필요}
- 성능: {예: 1만 라인 처리 1초 이내 / 없음}

## 배포 채널
- 채널: {예: PyPI / npm / cargo / Homebrew / 직접 바이너리}
- 버전 정책: {SemVer / CalVer}
- breaking change 정의: {예: CLI 옵션 제거, 함수 시그니처 변경}

## 성공 기준
{측정 가능 지표 (예: 다운로드 N건/월, 외부 사용자 사례 1건 이상)}
